const ownerModel = require('../../models/hehi/owner');

const owner = {

    async addOwner() {
        let result = {};
        let res = await ownerModel({

        });
        if(res.protocol41) {
            
        }
    }
}